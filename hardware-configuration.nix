# PLACEHOLDER. nixos-anywhere overwrites this at install time via
# `--generate-hardware-config nixos-generate-config ./hosts/homelab/hardware-configuration.nix`.
# Commit the generated version afterward. Disko provides all fileSystems, so this
# file only needs to carry kernel modules / microcode after generation.
{ ... }:
{ }
